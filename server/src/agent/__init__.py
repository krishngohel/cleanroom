"""Cleanroom Agent — an on-prem agentic task runner.

Gives every Cleanroom deployment the same kind of capability Claude Cowork
and Claude in Chrome provide, but entirely inside the customer's network:
the agent plans a task, works through it with tools (files, intranet pages,
connectors, workflows), streams progress to the dashboard, and logs every
action to the immutable audit trail.
"""
